# Smart Civil Engineering App

Aplikasi perhitungan teknik sipil berbasis Python & AI.

## Cara Menjalankan
1. Install library: `pip install -r requirements.txt`
2. Buat file `.env` dan isi: `GEMINI_API_KEY=Isi_Key_Disini`
3. Jalankan: `streamlit run app_utama.py`